<UserControl x:Class="PerfAnalyzer.FrameTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PerfAnalyzer"
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1300">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="731*"/>
      <ColumnDefinition Width="269*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="33*"/>
      <RowDefinition Height="467*"/>
    </Grid.RowDefinitions>
    <oxy:PlotView x:Name="FrameTime" Model="{Binding Model}" Grid.Row="1" Grid.Column="0" Margin="10"/>

    <DataGrid x:Name="dataGrid" Grid.Column="1" Margin="10" Grid.Row="1" ItemsSource="{Binding NodeList}"
              AutoGenerateColumns="False" SelectedItem="{Binding SelectedNode}" IsReadOnly="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="170" />
        <DataGridTextColumn Header="AvgExcl" Binding="{Binding AvgExclusiveTime, StringFormat=\{0:F2\}}" Width="80" SortDirection="Descending"/>
        <DataGridTextColumn Header="TotalExcl" Binding="{Binding TotalExclusiveTime, StringFormat=\{0:F2\}}" Width="80"/>
      </DataGrid.Columns>
    </DataGrid>
    <Slider x:Name="DownsampleLimit" HorizontalAlignment="Left" Margin="275,10,0,0" VerticalAlignment="Top" Width="141" Height="22" Minimum="10" Maximum="2000"/>
    <CheckBox x:Name="DownsampleGraph" Content="Downsample Graph" HorizontalAlignment="Left" Margin="135,10,0,0" VerticalAlignment="Top" Height="16" Width="124"/>
  </Grid>
</UserControl>
